import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

const Completion = () => {
  return <Box sx={{ marginX: "auto", marginTop: "5rem" }}>
    <Typography variant="h4" component="h1">Thank you for shopping</Typography> 
  </Box>;
};

export default Completion;
